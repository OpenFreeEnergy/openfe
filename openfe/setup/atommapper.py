class AtomMapper:
    def suggest_mappings(self, molA, molB):
        # for x in possible_mappings:
        #     yield x
        pass

    # don't make staticmethod to enable caching of score?
    def score_mapping(self, mapping):
        return 0.0
