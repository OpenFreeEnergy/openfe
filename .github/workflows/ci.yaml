name: "CI"
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  schedule:
    # At 07:00 UTC on Monday and Thursday.
    - cron: "0 7 * * 1,4"

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true

defaults:
  run:
    shell: bash -l {0}

jobs:
  tests:
    runs-on: ${{ matrix.OS }}-latest
    name: "tests"
    strategy:
      fail-fast: false
      matrix:
        os: ['ubuntu', 'macos']
        python-version:
          - "3.9"
          - "3.10"
        exclude:
          - os: 'macos'
            python-version: '3.10'

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # More info on options: https://github.com/conda-incubator/setup-miniconda
      - name: Setup Micromamba
        uses: mamba-org/provision-with-micromamba@main
        with:
          environment-file: environment.yml
          environment-name: openfe
          extra-specs: |
            python==${{ matrix.python-version }}

      - name: "Install"
        run: python -m pip install --no-deps -e .

      - name: "Test imports"
        run: |
          # if we add more to this, consider changing to for + env vars
          python -Ic "import openfe; print(openfe.__version__)"

      - name: "Environment Information"
        run: |
          micromamba info
          micromamba list

      - name: "Run tests"
        run: |
          pytest -n 2 -v --cov=openfe --cov=openfecli --cov-report=xml

      - name: codecov
        if: ${{ github.repository == 'OpenFreeEnergy/openfe'
                && github.event_name != 'schedule' }}
        uses: codecov/codecov-action@v3
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          file: coverage.xml
          fail_ci_if_error: False
          verbose: True

  package-tests:
    runs-on: ubuntu-latest
    name: "package install tests"
    steps:
      - uses: actions/checkout@v3

      - name: Setup Micromamba
        uses: mamba-org/provision-with-micromamba@main
        with:
          environment-file: environment.yml
          environment-name: openfe
          extra-specs: python==3.10

      - name: "Install"
        run: python -m pip install .

      - name: "Tests"
        # Move far out of the openfe src directory
        working-directory: ../../
        run: |
          pytest -v --pyargs openfe.tests
          pytest -v --pyargs openfecli.tests
